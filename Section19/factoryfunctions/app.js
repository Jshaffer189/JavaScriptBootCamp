function hex(r, g, b) {
	return '#' + ((1 << 24) + (r << 16) + (g << 8).toString(16).slice(1));
}
function rgb() {}

hex(255, 100, 25);

function makeColor(r, g, b) {
    const color = {};
    color.r = 
	return color;
}
