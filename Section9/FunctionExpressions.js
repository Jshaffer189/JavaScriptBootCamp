/*
Named functions stored inside variables
Functions are Objects

*/

const square = function(num, number) {
	return num * number;
};

console.log(square(3, 4));

function triangle(a, b) {
	return a + b;
}

console.log(triangle(3, 4));
